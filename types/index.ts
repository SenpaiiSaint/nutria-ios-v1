export type PantryItem = {
    id: string
    name: string
    quantity: number
    unit: string
  }
  
  export type Meal = {
    id: string
    name: string
    protein: number
    carbs: number
    fat: number
    date: string        // YYYY-MM-DD
  }
  
